<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.eju.demomodule">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        tools:replace="android:name">
        <activity
            android:name=".view.ContactActivity"
            android:label="联系人列表"
            android:exported="false" />
        <activity
            android:name=".view.RoomDemoActivity"
            android:exported="false" />
        <activity
            android:name=".view.DemoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.BigImageActivity"
            android:label="大图" />
        <activity
            android:name=".view.WebActivity"
            android:label="webPage" />
        <activity
            android:name=".view.ARouterDemoActivity"
            android:label="ARouterDemo" />
    </application>

</manifest>